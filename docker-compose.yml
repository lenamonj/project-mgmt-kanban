services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/app.sqlite3
    volumes:
      - pmdata:/app/data

volumes:
  pmdata:
