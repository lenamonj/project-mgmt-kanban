# Copy to .env and fill in. .env is gitignored.

# Required: OpenRouter API key for the AI assistant (https://openrouter.ai/keys)
OPENROUTER_API_KEY=

# Optional: signing key for the session cookie. A development default is used if unset.
SESSION_SECRET=
